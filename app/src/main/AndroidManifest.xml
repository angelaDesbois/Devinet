<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devinet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MenuActivity"></activity>
        <activity android:name=".activity.AproposActivity" />
        <activity android:name=".activity.ParametreActivity" />
        <activity android:name=".activity.testActivity" />
        <activity android:name=".activity.ProposerActivity" />
        <activity android:name=".activity.ResultatActivity" />
        <activity android:name=".activity.JeuxDetailMot" />
        <activity android:name=".activity.MenuIntentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ListeMotActivity" />
        <activity android:name=".activity.ListeCategorieActivity" />
        <activity android:name=".activity.MainActivity" />
    </application>

</manifest>